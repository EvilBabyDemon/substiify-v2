#install with "pip3 install -r requirements.txt"

discord.py[voice]
asyncpg
pytz
toml
colorlog
psutil
wavelink
matplotlib
pytest-playwright
#jishaku  # for debugging, not needed for normal use
